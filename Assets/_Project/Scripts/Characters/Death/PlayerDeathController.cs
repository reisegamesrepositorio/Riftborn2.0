using System;
using UnityEngine;

namespace Riftborn.Characters.Death
{
    [Obsolete("Player death is coordinated by PlayerController. This legacy component is kept only so migrated assets can be cleaned safely.")]
    public sealed class PlayerDeathController : MonoBehaviour
    {
    }
}